class Book {
    constructor(title, author, isbn, largo, ancho, alto) {
      this.title = title;
      this.author = author;
      this.isbn = isbn;
      this.largo = largo;
      this.ancho = ancho;
      this.alto = alto;
    }
  }
  
  export default Book;